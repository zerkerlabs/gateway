{
  "name": "@zerkerlabs/gateway-console-server",
  "version": "0.0.0",
  "description": "Backend-for-frontend for the Zerker Gateway operator console: OIDC login, server-side sessions, and a same-origin gateway proxy.",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.2.10"
  },
  "engines": {
    "node": ">=22"
  }
}
